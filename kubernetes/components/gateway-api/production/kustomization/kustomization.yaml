# Experimental channel: Cilium 1.20's Gateway API controller requires TLSRoute/BackendTLSPolicy/ReferenceGrant(v1), absent from the standard channel.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.6.1/experimental-install.yaml
