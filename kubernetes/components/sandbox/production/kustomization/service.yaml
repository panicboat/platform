apiVersion: v1
kind: Service
metadata:
  name: sandbox
  labels:
    app.kubernetes.io/name: sandbox
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sandbox
  ports:
    - name: http
      port: 8080
      targetPort: http
