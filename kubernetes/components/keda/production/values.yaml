# KEDA values for production

# =============================================================================
# Resources / Replicas
# =============================================================================
# chart default を採用 (HA / resource tuning は現状不要、AWS scaler の IRSA も
# 利用が顕在化していないため未設定)。
