apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-cd
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/panicboat/kubernetes-clusters
    targetRevision: panicboat-patch-1
    path: argo-cd
    helm:
      valueFiles:
      - values/common.yaml
      - values/minikube.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
